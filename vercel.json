{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm install",
  "buildCommand": "npm run circuits:fetch --workspace=frontend && npm run build --workspace=frontend",
  "outputDirectory": "frontend/.next"
}
